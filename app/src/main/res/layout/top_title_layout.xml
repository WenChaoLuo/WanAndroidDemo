<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/top_title_layout_bg"
    android:id="@+id/top_title_layout"
    android:elevation="@dimen/dp_2"
    android:layout_height="70dp">
        <RelativeLayout
            android:id="@+id/id_top_layout_back_layout"
            android:layout_centerVertical="true"
            android:clickable="true"
            android:focusable="true"
            android:paddingStart="15dp"
            android:paddingEnd="15dp"
            android:background="@drawable/top_click_layout_bg"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/dp_50"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            >
            <ImageView
                android:layout_width="20dp"
                android:layout_height="28dp"
                android:layout_centerInParent="true"
                android:src="@drawable/ic_back"
                android:id="@+id/id_top_layout_back_img"
                />
        </RelativeLayout>
        <TextView
            android:id="@+id/id_top_layout_title_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textColor="@color/first_text_color"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="@id/id_top_layout_back_layout"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="标题"
            android:textSize="@dimen/second_text_size"/>

</android.support.constraint.ConstraintLayout>